name: phonetic_speech_recognizer_example
description: "Demonstrates how to use the phonetic_speech_recognizer plugin."
environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_sound: ^9.28.0
  path_provider: ^2.1.2

  phonetic_speech_recognizer:
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true

